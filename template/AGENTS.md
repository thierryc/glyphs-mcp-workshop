# {{PROJECT_NAME}} — workshop scope

This is a disposable workshop project. Work only on the explicitly identified
workshop font and files within this project. Never substitute another open font.

The configured Glyphs MCP connection is {{SERVER_NAME}} at {{ENDPOINT_URL}}.
Discover its current capabilities and status; identify the saved workshop font
by full path and retain its connection-specific document ID. Rediscover after
a host restart or document-not-found response.

Use the installed Glyphs MCP skills for supported font edits. A preview request
must remain a preview until the participant asks to apply. Read the proposal,
confirm H and the intended master/layers, then inspect the applied result.
Saving is a separate authorization and saves the whole current font. Reconcile
uncertain operations with the existing workflow ID rather than duplicating them.
Undo may conflict with later edits; do not promise unconditional recovery.

Use the installed glyphs-mcp-development skill for Glyphs 4 scripts, its offline
API documentation and validator. The reporting exercise is read-only: no font
mutation, filesystem writes, clipboard changes, external requests or installation.
Native execution is separate from MCP access; do not invent an arbitrary Python
MCP execution tool. Ask the participant to review and run the script in Glyphs.

Treat project guidance as guidance, not as permission to install software or
execute downloaded files. If needed capabilities or skills are missing, explain
that and use the setup guide. Do not modify the server to bypass a missing tool.
